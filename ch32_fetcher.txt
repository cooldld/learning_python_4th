def fetcher(obj, index)

x = spam
fetcher(x, 3) = m

try/except 捕获异常
try fetcher(x, 4)
exception: IndexError

异常处理完，后面的代码继续执行
hello

try/finally 终止行为
try fetcher(x, 3)
exception: finally
try fetcher(x, 3) finish

try/finally 终止行为
try fetcher(x, 4)
exception: finally
